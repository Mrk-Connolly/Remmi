package com.remmi.app.core

/**
 * Root interface for all core components within the Remmi architecture.
 *
 * By having a single root interface, we ensure that every major component
 * (Plugins, Models, Actions, Widgets, etc.) can be treated as a part of the
 * unified Remmi ecosystem.
 */
interface RemmiClass {
}
