package com.remmi.app.core.eventBus

/**
 * Standard standard event types for the Remmi system.
 */
enum class EventType {
    CREATED,
    UPDATED,
    DELETED,
    LOADED,
    STARTED,
    STOPPED,
    ERROR
}
